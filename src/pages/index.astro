---
import { PasswordGenerator } from '~/components/PasswordGenerator'
import Header from '~/components/Header.astro'
import Footer from '~/components/Footer.astro'
import { generatePassword } from '~/utils/generate-password'
import { DEFAULT_KEYS as pwdDict, DEFAULT_PASSWORD_LENGTH as pwdLength } from '~/utils/constants'
import Layout from '@layout'

const initialPassword = generatePassword(pwdLength, pwdDict)
---

<Layout title='Generador de contraseñas'>
	<div
		class='flex flex-col items-center justify-center min-h-[100vh] text-white w-full bg-white gap-4 pt-16 pb-8 px-4'
	>
		<Header />
		<PasswordGenerator initialPassword={initialPassword} client:load />
		<Footer />
	</div>
</Layout>
